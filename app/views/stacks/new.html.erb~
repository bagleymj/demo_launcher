  <div class="row">
    <div class="col-md-6">
      <%= form_for @stack do |f| %>
        <%= f.label :template_id %>
        <%= f.collection_select(:template_id, @templates, :id, :template_name, class: "form-control") %>
        <%= f.label :stack_name %>
        <%= f.text_field :stack_name, class: "form-control" %>
        <%= f.submit "Launch Environment", class: "btn btn-success" %>
        <% if !flash[:danger].nil? %>
          <% flash[:danger].each do |message| %>
            <p class="text-danger"><%= message %></p>
          <% end %>
        <% end %>
      <% end %>
    </div>
  </div>

