<div class="row">
  <table class="table table-striped">
    <tr>
      <th>
        Company Name
      </th>
    </tr>
    <% @companies.each do |company| %>
      <tr>
        <td>
          <%= company.company_name %>
        </td>
      </tr>
    <% end %>
  </table>
  <%= link_to "Create New Company", :action => :new %>
</div>
