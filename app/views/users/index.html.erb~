<% if flash[:danger] != nil %>
  <p class="text-danger"><%= flash[:danger] %></p>
<% end %>
<table class="table table-striped">
  <tr>
    <th>
      User Name
    </th>
    <th>
      Display Name
    </th>
    <th>
      Company Name
    </th>
    <th>
      Actions
    </th>
  </tr>
  <% @users.each do |user| %>
    <tr>
      <td>
        <%= user.username %>
      </td>
      <td>
        <%= user.display_name %>
      </td>
      <td>
        <%= user.company_name %>
      </td>
      <td>
        <%= link_to "Edit", edit_user_path(user.id) %>|<%= link_to "Delete", user_path(user.id),
          method: 'DELETE',
          confirm: "Are you sure you want to delete #{user.display_name}?" %>
      </td>
    </tr>
  <% end %>
</table>
<%= link_to "Add New User", new_user_path %>

